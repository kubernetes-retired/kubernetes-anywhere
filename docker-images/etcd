FROM gcr.io/google_containers/etcd:2.0.13
LABEL works.weave.role=system

CMD p="http://$(hostname -s)" ; \
  etcd --name=$(hostname -s) \
    --listen-peer-urls $p:2380 \
    --initial-advertise-peer-url $p:2380 \
    --listen-client-urls $p:4001 \
    --advertise-client-urls $p:4001
